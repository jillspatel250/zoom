backend ma socket.io joie and frontend ma socket-client joiee

mongodb+srv://jillspatel250:<jills@2022>@cluster0.puiqu.mongodb.net/

for implementing socket apde createServer and server e module import kayra che 
### apdu express nu instance and socket nu server banne alag che toh banne ne connect karva padse
### to e connect apde createServer vade kariee

const app=express();
const server=createServer(app);//app ne server thi connect kayru 
const io=connectToSocket(server);//have io socket ne init karsu and ene server jode connect karsu..


app.use(cors());->http vadi error avi jay cross-origin ni toh ene takle karva we use cors
app.use(express.json({"limit":"40kb"}));->vadhare payload na jato re etle we set limit option

export  {User,Admin};->ek j file mathi multiple vastu export karvi hoy toh {} aa use
export default User;->ek j file mathi ek j vastu export karvi hoy toh we use export default;
